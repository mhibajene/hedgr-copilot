# Trust Phrase Allowlist
# ---------------------
# Substring exceptions (case-insensitive): if a flagged line also
# contains one of these substrings, the match is skipped.
#
# File exceptions: prefix with "file:" to skip an entire file.
# Path is relative to repo root.
#
# Prefer file: exemptions over substrings — they are narrower and
# cannot accidentally suppress user-facing copy in other files.

# --- File exceptions ---

# The copilot policy engine defines the runtime urgency blacklist.
# It must reference banned phrases to enforce them.
file:apps/frontend/lib/server/copilotPolicy.ts

# Policy config files that declare bannedClaims arrays containing
# banned phrase tokens (the definitions, not user-facing copy).
file:apps/frontend/lib/policy/defaults.ts
file:apps/frontend/lib/policy/overlays.ts
