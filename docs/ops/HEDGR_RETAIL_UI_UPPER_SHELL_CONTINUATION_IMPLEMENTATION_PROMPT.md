# HEDGR RETAIL UI UPPER SHELL CONTINUATION IMPLEMENTATION PROMPT

Status: Implementation-planning artifact only  
Authority: Non-shipping, non-implementation-authorizing; repo-native authority remains binding  
Scope: Cursor execution prompt for a tightly bounded upper-shell continuity refinement pass adjacent to the consolidated retail header direction  
Last updated: 2026-04-21

---

## 1. Purpose

This artifact translates the governed upper-shell next scope into an implementation-oriented Cursor prompt for the Hedgr retail UI lane.

It is specifically for the adjacent upper-shell continuation beneath the consolidated header direction, not for reopening header strategy.

Primary scope inheritance source:

- `docs/ops/HEDGR_RETAIL_UI_UPPER_SHELL_NEXT_SCOPE_NOTE.md`

Governing context sources for consistency:

- `docs/ops/HEDGR_RETAIL_UI_UPPER_SHELL_NEXT_SCOPE_NOTE.md`
- `docs/ops/HEDGR_RETAIL_UI_HEADER_DIRECTION_CONSOLIDATION_NOTE.md`
- `docs/ops/HEDGR_RETAIL_UI_HEADER_PROTOTYPE_PASS_READOUT.md`
- `docs/ops/HEDGR_RETAIL_UI_HEADER_REFINEMENT_PHASE_3_DECISION_NOTE.md`
- `docs/ops/HEDGR_RETAIL_UI_PHASE_3_TO_PROTOTYPE_GATE_NOTE.md`
- `docs/ops/HEDGR_STATUS.md`
- `AGENTS.md`

---

## 2. Authority and implementation boundary

- Repo-native authority remains binding.
- `docs/ops/HEDGR_STATUS.md` and `AGENTS.md` govern what implementation is actually authorized.
- This artifact does not itself authorize shipping, route rollout, or implementation beyond repo-approved scope.
- This artifact must not reopen header direction, Family C role, or broader shell strategy.
- If this artifact conflicts with repo-native authority, repo-native authority wins.

---

## 3. Inherited fixed decisions

The following decisions are inherited as fixed and must not be changed in this continuation pass:

- money-first shell direction
- balance-first header hierarchy
- calm attached subordinate trust
- actions close but secondary
- `Recent activity` naming centerline
- Family C visible but subordinate
- Variant A / tight money-anchor cluster retained
- rejection of portfolio / monitoring / crypto-dashboard drift
- rejection of earn-first / APY-first framing

---

## 4. Implementation target

Target only the adjacent upper-shell continuation zone below and around the consolidated header, including:

- transition from header into `Recent activity`
- spacing continuity between header, action cluster, trust continuation, and first body block
- subordinate Family C continuation treatment
- first-body-block composure

This is a continuity refinement zone, not a redesign zone.

---

## 5. Bounded implementation-change envelope (if authorized)

If implementation is authorized by repo-native authority, the pass should remain limited to local, bounded UI adjustments:

- spacing calibration
- alignment refinement
- section boundary cleanup
- hierarchy continuity adjustments
- local layout polish that improves continuity
- small visual restraint improvements

Changes should remain incremental, reversible, and narrow.

---

## 6. Explicitly prohibited changes

The following are out of scope and prohibited in this pass:

- header redesign
- CTA redesign
- Family C redesign
- allocation pattern redesign
- new trust-system constructs
- shell restructuring
- new dashboard sections
- new copy strategy
- implementation of new business logic
- backend / engine / accounting-truth implications

---

## 7. Files and modules to inspect first (if authorized)

If implementation is authorized, Cursor should first inspect the current retail/dashboard/header/upper-shell implementation surfaces and confirm exact file paths in the repo.

Start with repo-native discovery for:

- `apps/frontend/` retail/dashboard route files that render the upper shell and first body transition
- shared UI components used by those route surfaces (including upper-shell/header-adjacent composition)
- styles, tokens, and layout utilities consumed by the active route and its upper-shell components

Execution requirements for any authorized pass:

- confirm actual file locations before changes
- follow `AGENTS.md` conventions exactly
- prefer incremental changes over rewrites

---

## 8. Implementation guidance for Cursor

For any authorized implementation pass, Cursor should execute with the following discipline:

1. Identify the minimum file set required for the continuity refinement.
2. Describe the current implementation shape before editing (composition, spacing relationships, and continuity breaks).
3. Make the smallest change set that improves upper-shell continuity.
4. Preserve the fixed header centerline and avoid introducing any new structural concept.
5. Call out edge cases for small mobile viewports where continuity can collapse or over-segment.
6. Note rollback-safe boundaries (what can be reverted cleanly if side effects appear).

No speculative expansion beyond the bounded target zone is allowed.

Reopen guardrail:

- this prompt does not authorize reopening the settled header or upper-shell direction
- any reopen assessment must remain evidence-based and governed
- invalid reopen grounds include novelty-seeking, preference drift, aesthetic restlessness, and generalized desire for stronger expression
- if repo-native authority or later governed artifacts indicate a real reopen trigger, repo-native authority decides

---

## 9. Acceptance checks

A candidate implementation pass must satisfy all of the following:

- header still reads balance-first
- transition into `Recent activity` feels more continuous
- actions remain secondary
- Family C remains subordinate and non-instrumental
- upper shell feels less segmented and less dashboard-like
- no prohibited structural or copy drift occurred

Failure of any check means the pass is not acceptable as-is.

---

## 10. Evidence and review expectations

Any implementation response generated from this prompt must provide:

- files changed
- summary of what changed and why
- confirmation of what remained fixed
- risks and trade-offs
- whether any boundary pressure was encountered
- rollback considerations
- screenshots or visual before/after notes if available

Evidence should prioritize reviewability and boundary compliance over narrative volume.

---

## 11. Working conclusion

If and only if repo-native authority authorizes implementation, this work should behave as a bounded continuity refinement pass beneath the settled header.

It must be executed and reviewed as a constrained upper-shell continuation adjustment, not as a redesign exercise.
