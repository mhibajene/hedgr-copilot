# Stability Engine trust surface — automated test coverage matrix (MC-S3-011)

**Status:** Governance evidence artifact only. This document maps **shipped** Stability Engine trust surfaces documented in [`HEDGR_STATUS.md`](HEDGR_STATUS.md) **§6** to **existing** automated tests in-repo. It is **not** authorization to start work, **not** a backlog, and **not** a sequencing signal. It does **not** imply doctrine, ADR, or runtime boundary changes.

**Scope:** Stability Engine–scoped **§6** merged truth only (engine contract, dashboard engine cluster, `apps/frontend/lib/engine/` copy and helpers tied to those tickets). **§6** rows whose primary implementation is withdraw lifecycle, transaction mock-state seam, or market-data continuity **are omitted** from this matrix by default; they are not Stability Engine surfaces for this artifact. No exploratory-only mockups, Paper variants, or retired concepts appear as rows.

**Source of truth for shipped behavior:** [`docs/ops/HEDGR_STATUS.md`](HEDGR_STATUS.md) **§6**.

---

## Coverage class definitions (present-state only)

| Class | Meaning |
|--------|---------|
| **Covered** | Existing Vitest and/or Playwright tests clearly exercise the surface or module (behavioral and/or copy-contract tests tied to that surface). |
| **Partially covered** | Automated linkage exists but is indirect, narrow, or split across files such that the full surface is not uniformly exercised in one place. |
| **Uncovered** | No dedicated automated test artifact reasonably tied to this surface; any linkage is incidental or type-level only. |

---

## Matrix

| §6 grounding | Surface / module | Coverage class | Rationale |
|--------------|------------------|----------------|-----------|
| `MC-S2-001` | Canonical **`EngineState`** / **`EnginePosture`** contract (`apps/frontend/lib/engine/types.ts`) | **Covered** | [`apps/frontend/__tests__/engine-types-contract.test.ts`](../../apps/frontend/__tests__/engine-types-contract.test.ts) locks the canonical posture union, required `EngineState` keys, optional `notice`, and `EngineNotice` shape. |
| `MC-S2-002`, `MC-S4-002` | Mock engine state provider (`mock.ts`), **`useEngineState`**, local posture simulator resolver | **Covered** | [`apps/frontend/__tests__/use-engine-state.test.tsx`](../../apps/frontend/__tests__/use-engine-state.test.tsx) exercises the hook and query/local guard behavior. [`apps/frontend/__tests__/engine-simulator.test.ts`](../../apps/frontend/__tests__/engine-simulator.test.ts) exercises simulator resolver paths per **§6** `MC-S4-002` / `MC-S4-003`. [`apps/frontend/__tests__/engine-notices-and-mock-contract.test.ts`](../../apps/frontend/__tests__/engine-notices-and-mock-contract.test.ts) asserts **`getMockEngineState`** notice wiring. |
| `MC-S4-003` | Initial Stability Engine trust-surface test envelope (posture presentation, notice visibility, simulator seam, `useEngineState` trust seam) | **Covered** | **§6** `MC-S4-003` implementation truth is satisfied by the existing suite: posture and banner behavior in [`apps/frontend/__tests__/engine-posture-header.test.tsx`](../../apps/frontend/__tests__/engine-posture-header.test.tsx), simulator behavior in [`apps/frontend/__tests__/engine-simulator.test.ts`](../../apps/frontend/__tests__/engine-simulator.test.ts), hook seam in [`apps/frontend/__tests__/use-engine-state.test.tsx`](../../apps/frontend/__tests__/use-engine-state.test.tsx). This row records the umbrella ticket; file-level detail is in adjacent rows. |
| `MC-S2-003`, `MC-S2-006` | Dashboard **posture header** — badge, notice banner, one-line posture context (`ENGINE_POSTURE_CONTEXT`) | **Covered** | [`apps/frontend/__tests__/engine-posture-header.test.tsx`](../../apps/frontend/__tests__/engine-posture-header.test.tsx) covers badge labels, `normal` banner absence, and non-`normal` banner presence per posture. Posture context strings are asserted there and in [`apps/frontend/__tests__/engine-posture-context-contract.test.ts`](../../apps/frontend/__tests__/engine-posture-context-contract.test.ts) (**§6** `MC-S2-006`). |
| `MC-S4-001`, `MC-S3-004` | Canonical Stability **notice copy** (`ENGINE_NOTICE_COPY`) and mock assembly of `EngineState.notice` | **Covered** | [`apps/frontend/__tests__/engine-notices-and-mock-contract.test.ts`](../../apps/frontend/__tests__/engine-notices-and-mock-contract.test.ts) locks non-`normal` notice objects and informational framing (**§6** `MC-S3-004`). |
| `MC-S2-004`, `MC-S3-005`, `MC-S3-009` | **Allocation bands** UI (`EngineAllocationBands`), informational targets, caption/legend, per-band descriptions | **Partially covered** | Strong coverage via [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx) (mount, copy, DOM structure, per-`bandDescription` nodes per **§6** `MC-S3-009`) and [`apps/frontend/__tests__/dashboard.page.test.tsx`](../../apps/frontend/__tests__/dashboard.page.test.tsx) (dashboard integration paths). Caption/legend framing extensions from **§6** `MC-S3-006` are exercised in the same files (see dedicated `MC-S3-006` row). **§6** `MC-S3-005` (Warmth v1) is presentation-only; there is no separate test module whose name identifies “warmth” — presentation shifts are tracked via these tests and related copy contracts. Present-state pattern: “warmth” is not isolated as its own test file name. |
| `MC-S2-009` | **Stability explainer** (`EngineStabilityExplainer`, `stability-explainer-copy.ts`) | **Covered** | [`apps/frontend/__tests__/engine-stability-explainer-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-stability-explainer-copy-contract.test.ts) (**§6** `MC-S3-007`) plus integration/assertions in [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx) and [`apps/frontend/__tests__/dashboard.page.test.tsx`](../../apps/frontend/__tests__/dashboard.page.test.tsx) per **§6** `MC-S2-009`. |
| `MC-S2-010` | **Protective guidance** (`EngineProtectiveGuidance`, `protective-guidance-copy.ts`) | **Covered** | [`apps/frontend/__tests__/engine-protective-guidance-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-protective-guidance-copy-contract.test.ts) (**§6** `MC-S3-007`) and mount/order coverage in [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx) per **§6** `MC-S2-010`. |
| `MC-S2-011`, `MC-S2-012`, `MC-S2-013`, `MC-S2-014` | **Stability review snapshot** cluster — snapshot UI, cadence cue, fingerprint, recent memory (`EngineStabilityReviewSnapshot`, fingerprint/memory helpers, `stability-review-snapshot-copy.ts`) | **Covered** | Component and behavior: [`apps/frontend/__tests__/engine-stability-review-snapshot.test.tsx`](../../apps/frontend/__tests__/engine-stability-review-snapshot.test.tsx). Fingerprint and memory logic: [`apps/frontend/__tests__/review-snapshot-fingerprint.test.ts`](../../apps/frontend/__tests__/review-snapshot-fingerprint.test.ts), [`apps/frontend/__tests__/review-snapshot-memory.test.ts`](../../apps/frontend/__tests__/review-snapshot-memory.test.ts). Copy contract: [`apps/frontend/__tests__/engine-stability-review-snapshot-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-stability-review-snapshot-copy-contract.test.ts) (**§6** `MC-S3-008`). Integration: [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx), [`apps/frontend/__tests__/dashboard.page.test.tsx`](../../apps/frontend/__tests__/dashboard.page.test.tsx). Rows are combined because **§6** places cadence, change signal, and memory inside the same snapshot presentation path. |
| `MC-S3-006` | Regression extension — dashboard snapshot paths, posture context contract module, trust-framing denylist helper, allocation caption/legend guards | **Covered** | **§6** lists [`apps/frontend/__tests__/dashboard.page.test.tsx`](../../apps/frontend/__tests__/dashboard.page.test.tsx), [`apps/frontend/__tests__/engine-posture-context-contract.test.ts`](../../apps/frontend/__tests__/engine-posture-context-contract.test.ts), [`apps/frontend/__tests__/engine-trust-framing-denylist.ts`](../../apps/frontend/__tests__/engine-trust-framing-denylist.ts), extended [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx). |
| `MC-S3-007` | Vitest copy contracts for protective guidance and stability explainer (non-empty segments, shared denylist pattern) | **Covered** | [`apps/frontend/__tests__/engine-protective-guidance-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-protective-guidance-copy-contract.test.ts), [`apps/frontend/__tests__/engine-stability-explainer-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-stability-explainer-copy-contract.test.ts) per **§6** `MC-S3-007`. |
| `MC-S3-008` | Vitest copy contract for **`stability-review-snapshot-copy`** exports | **Covered** | [`apps/frontend/__tests__/engine-stability-review-snapshot-copy-contract.test.ts`](../../apps/frontend/__tests__/engine-stability-review-snapshot-copy-contract.test.ts) per **§6** `MC-S3-008`. |
| `MC-S3-009` | Per-band **`bandDescription`** output and word guards on allocation band description nodes | **Covered** | [`apps/frontend/__tests__/engine-allocation-bands.test.tsx`](../../apps/frontend/__tests__/engine-allocation-bands.test.tsx) per **§6** `MC-S3-009`. |

---

## Evidence notes (factual, present-state)

- **§6 tickets merged into combined rows:** `MC-S2-011`–`MC-S2-014` share one snapshot presentation path in **§6**; the matrix uses one row to match how tests overlap (`engine-stability-review-snapshot.test.tsx`, fingerprint/memory tests, copy contract).
- **Regression tranches `MC-S3-004`–`MC-S3-009`:** Coverage is represented both in dedicated contract rows (`MC-S3-004` with `MC-S4-001`; `MC-S3-007`–`MC-S3-009`) and in the allocation/review integration rows above to avoid duplicate claims about the same files.
- **Omitted §6 areas (by matrix scope, not absence of tests):** Withdraw lifecycle and subordinate strips (**§6** `MC-S2-008`, `MC-S2-015`–`MC-S2-019`), market-data continuity (**§6** `MC-S2-020`), transaction review simulator seam (**§6** `MC-S2-021`–`MC-S2-022`) have their own **§6** test references; they are out of scope for *this* Stability Engine matrix unless governance expands the artifact’s scope.

---

## Maintenance

When **§6** merged truth changes, update this matrix in the same merge window if governance relies on it for evidence continuity. Do not treat this file as a substitute for **`HEDGR_STATUS.md` §7** / **§7a** for approved work.
