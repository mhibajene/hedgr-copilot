{
  "name": "hedgr-copilot",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build:ui": "pnpm --filter @hedgr/ui run build",
    "build": "pnpm run build:ui && turbo run build"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}