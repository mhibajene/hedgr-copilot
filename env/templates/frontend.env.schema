NEXT_PUBLIC_APP_ENV=
API_BASE_URL=
POSTHOG_KEY=
SENTRY_DSN=
# Balance SSoT: When true (default), balances are computed from ledger only
# Set to 'false' to rollback to legacy wallet store behavior
NEXT_PUBLIC_BALANCE_FROM_LEDGER=true
